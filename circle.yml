
general:
  branches:
    only:
      - master

  ruby:
    version:
      2.2.0

dependencies:
  post:
    - bundle exec jekyll build:
        timeout: 300 # 5 min

test:
  override:
    - echo "No tests are needed"